# Fix install.sh

When run browser4-cli/install.sh, there is problem to determine the latest Browser4 release tag.

```
[browser4-cli installer] Java 21 is already installed.
[browser4-cli installer] Google Chrome is already installed.
[browser4-cli installer] Rust toolchain already installed.
[browser4-cli installer] ERROR: Unable to determine the latest Browser4 release tag.
```

Fix the issue and simplify the installer if possible.
