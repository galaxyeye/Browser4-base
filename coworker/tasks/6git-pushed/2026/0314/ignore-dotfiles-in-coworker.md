# Refine Coworker

Check the coworker that checks directories for changes and runs commands when changes are detected.

Add the following features:

1. Always ignore files starts with a dot (e.g., .git, .env).
